#include <stdio.h>

int main()
{
	int minutes;
	
	/* Calculating the total number of minutes in a year */
	minutes = 365 * 24 * 60;
	
	/* Displaying the result on the screen */
	printf("The number of minutes in a year is: %d\n", minutes);
	
	return 0;
}
